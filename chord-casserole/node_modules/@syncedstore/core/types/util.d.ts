/**
 * Filter a SyncedStore array
 * @param arr array to filter
 * @param filter predicate to filter the array `arr` by
 */
export declare function filterArray<T>(arr: T[], filter: (obj: T) => boolean): void;
