const tt = ["data-playhtml-hover", "data-playhtml-focus"], Et = [
  "__playhtml-element",
  "ph-flash",
  "ph-inspect-highlight",
  "ph-inspect-highlight-hover",
  "ph-inspect-selected",
  "playhtml-loading"
], wt = {
  "aria-busy": "true",
  "aria-live": "polite"
}, St = {
  defaultData: (t) => m(t),
  myDefaultAwareness: { hover: !1, focus: !1 },
  onMount: ({ getElement: t, setData: e, setMyAwareness: r }) => {
    const n = t(), o = e, i = At(n, (a) => {
      const l = a.filter(
        (u) => u.type !== "attributes" || !tt.includes(u.attributeName || "")
      );
      l.length !== 0 && o((u) => {
        vt(u, l);
      });
    });
    n.__playhtml_observer = i, n.addEventListener("mouseenter", () => {
      r({ hover: !0, focus: n.hasAttribute("data-playhtml-focus") }), n.setAttribute("data-playhtml-hover", "");
    }), n.addEventListener("mouseleave", () => {
      r({ hover: !1, focus: n.hasAttribute("data-playhtml-focus") }), n.removeAttribute("data-playhtml-hover");
    }), n.addEventListener("focusin", () => {
      r({ hover: n.hasAttribute("data-playhtml-hover"), focus: !0 }), n.setAttribute("data-playhtml-focus", "");
    }), n.addEventListener("focusout", () => {
      r({ hover: n.hasAttribute("data-playhtml-hover"), focus: !1 }), n.removeAttribute("data-playhtml-focus");
    });
    const s = (a, l) => {
      if (a.formState && (l.formState = a.formState), a.children && l.children)
        for (let u = 0; u < a.children.length; u++) {
          const f = a.children[u];
          f.nodeType === "HTMLElement" && l.children[u] && s(
            f,
            l.children[u]
          );
        }
    }, c = () => {
      o((a) => {
        const l = m(n);
        s(l, a), l.children && a.children.splice(0, a.children.length, ...l.children);
      });
    };
    n.addEventListener("input", c), n.addEventListener("change", c);
  },
  updateElement: ({ element: t, data: e }) => {
    const r = m(t);
    if (X(r, e))
      return;
    const n = t.__playhtml_observer;
    n && (n.takeRecords(), n.disconnect()), A(t, e), n && n.observe(t, {
      childList: !0,
      attributes: !0,
      subtree: !1,
      characterData: !0
    });
  },
  updateElementAwareness: ({ element: t, awareness: e }) => {
    const r = e.some((o) => o?.hover), n = e.some((o) => o?.focus);
    r ? t.setAttribute("data-playhtml-hover", "") : t.removeAttribute("data-playhtml-hover"), n ? t.setAttribute("data-playhtml-focus", "") : t.removeAttribute("data-playhtml-focus");
  }
};
function B(t) {
  return t.nodeType === "HTMLElement";
}
function X(t, e) {
  if (t.nodeType !== e.nodeType)
    return !1;
  if (t.nodeType === "Text" && e.nodeType === "Text")
    return t.textContent === e.textContent;
  if (B(t) && B(e)) {
    if (t.tagName !== e.tagName)
      return !1;
    const r = N(t.attributes), n = N(e.attributes);
    if (Object.keys(r).length !== Object.keys(n).length)
      return !1;
    for (const [a, l] of Object.entries(r))
      if (n[a] !== l)
        return !1;
    const o = t.formState, i = e.formState;
    if ((o || i) && (!o || !i || o.checked !== i.checked || o.value !== i.value || o.selectedIndex !== i.selectedIndex))
      return !1;
    const s = v(t.children), c = v(e.children);
    if (s.length !== c.length)
      return !1;
    for (let a = 0; a < s.length; a++)
      if (!X(s[a], c[a]))
        return !1;
  }
  return !0;
}
function At(t, e, r) {
  const o = { ...{
    childList: !0,
    attributes: !0,
    subtree: !1,
    characterData: !0
  }, ...r }, i = (c) => {
    const a = c.filter((l) => {
      if (l.target !== t)
        return !1;
      if (o.childList && l.type === "childList")
        return !0;
      if (o.attributes && l.type === "attributes")
        if (o.attributeFilter) {
          if (o.attributeFilter.includes(l.attributeName || ""))
            return !0;
        } else
          return !0;
      return !!(o.characterData && l.type === "characterData" || o.subtree && l.type === "childList");
    });
    e(a);
  }, s = new MutationObserver(i);
  return s.observe(t, o), s;
}
function vt(t, e) {
  e.forEach((r) => {
    switch (r.type) {
      case "attributes":
        xt(t, r);
        break;
      case "childList":
        Ct(t, r);
        break;
      case "characterData":
        Mt(t, r);
        break;
    }
  });
}
function xt(t, e) {
  if (t.nodeType !== "Text" && e.target instanceof HTMLElement) {
    const r = e.attributeName, n = e.target.getAttribute(r), o = H(e.target);
    if (n !== null) {
      const i = F(
        r,
        n,
        o
      );
      i === null ? r in t.attributes && delete t.attributes[r] : t.attributes[r] !== i && (t.attributes[r] = i);
    } else r in t.attributes && delete t.attributes[r];
  }
}
function Ct(t, e) {
  if (t.nodeType === "Text" || !(e.target instanceof HTMLElement))
    return;
  const r = [];
  e.target.childNodes.forEach((o) => {
    Y(o) && r.push(m(o));
  });
  const n = v(t.children);
  n.length === t.children.length && Pt(n, r) || t.children.splice(0, t.children.length, ...r);
}
function Mt(t, e) {
  const r = e.target;
  switch (t.nodeType) {
    case "Text":
      if (r instanceof Text)
        return t.textContent = r.textContent || "", !0;
      break;
  }
  return !1;
}
function Lt(t) {
  return t instanceof HTMLElement || t instanceof Text;
}
function et(t) {
  return t instanceof HTMLElement && t.classList.contains("ph-inspect-label");
}
function Y(t) {
  return Lt(t) && !et(t);
}
function k(t, e) {
  return !!t?.split(/\s+/).includes(e);
}
function Tt(t) {
  return t.nodeType === "HTMLElement" && k(t.attributes.class, "ph-inspect-label");
}
function v(t) {
  return t.filter((e) => !Tt(e));
}
function Pt(t, e) {
  return t.length !== e.length ? !1 : t.every(
    (r, n) => X(r, e[n])
  );
}
function rt(t, e) {
  const r = new Set(Et);
  return e && k(t, "playhtml-loading") && r.add(e), r;
}
function H(t) {
  const e = t.getAttribute("class") || void 0;
  return {
    classValue: e,
    localClassNames: rt(
      e,
      t.getAttribute("loading-class")
    )
  };
}
function It(t) {
  return {
    classValue: t.class,
    localClassNames: rt(
      t.class,
      t["loading-class"]
    )
  };
}
function nt(t, e, r) {
  return tt.includes(t) ? !0 : k(r.classValue, "playhtml-loading") && wt[t] === e;
}
function F(t, e, r) {
  return nt(t, e, r) ? null : t === "class" ? Nt(
    e,
    r.localClassNames
  ) || null : e;
}
function N(t) {
  const e = {}, r = It(t);
  for (const [n, o] of Object.entries(t)) {
    const i = F(n, o, r);
    i !== null && (e[n] = i);
  }
  return e;
}
function Nt(t, e) {
  return t.split(/\s+/).filter((r) => r && !e.has(r)).join(" ");
}
function ot(t, e) {
  return t.split(/\s+/).filter((r) => e.has(r)).join(" ");
}
function Ot(t, e, r) {
  const n = [
    ...t.split(/\s+/).filter(Boolean),
    ...ot(e, r).split(/\s+/).filter(Boolean)
  ];
  return Array.from(new Set(n)).join(" ");
}
function _t(t) {
  if (t instanceof HTMLInputElement) {
    const e = {};
    return t.type === "checkbox" || t.type === "radio" ? e.checked = t.checked : e.value = t.value, e;
  }
  if (t instanceof HTMLTextAreaElement)
    return { value: t.value };
  if (t instanceof HTMLSelectElement)
    return { selectedIndex: t.selectedIndex, value: t.value };
}
function m(t) {
  if (t instanceof Text)
    return {
      nodeType: "Text",
      textContent: t.textContent || ""
    };
  const e = {
    nodeType: "HTMLElement",
    tagName: t.tagName.toLowerCase(),
    attributes: {},
    children: []
  }, r = H(t);
  for (const o of t.attributes) {
    const i = F(
      o.name,
      o.value,
      r
    );
    i !== null && (e.attributes[o.name] = i);
  }
  const n = _t(t);
  return n && (e.formState = n), t.childNodes.forEach((o) => {
    Y(o) && e.children.push(m(o));
  }), e;
}
function Xt(t, e) {
  e && (t instanceof HTMLInputElement ? (t.type === "checkbox" || t.type === "radio") && e.checked !== void 0 ? t.checked = e.checked : e.value !== void 0 && (t.value = e.value) : t instanceof HTMLTextAreaElement && e.value !== void 0 ? t.value = e.value : t instanceof HTMLSelectElement && e.selectedIndex !== void 0 && (t.selectedIndex = e.selectedIndex));
}
function A(t, e) {
  Yt(t, e), e.nodeType === "HTMLElement" && (kt(t, e), Xt(t, e.formState), Ft(t, e));
}
function Yt(t, e) {
  e && e.nodeType === "Text" && t.textContent !== e.textContent && (t.textContent = e.textContent || "");
}
function kt(t, e) {
  if (!e)
    return;
  const r = e.attributes && typeof e.attributes == "object" ? N(e.attributes) : {}, n = H(t);
  for (const [o, i] of Object.entries(r)) {
    const s = o === "class" ? Ot(
      i,
      t.getAttribute("class") || "",
      n.localClassNames
    ) : i;
    t.getAttribute(o) !== s && t.setAttribute(o, s);
  }
  Array.from(t.attributes).forEach((o) => {
    if (!nt(o.name, o.value, n)) {
      if (o.name === "class") {
        const i = ot(
          o.value,
          n.localClassNames
        );
        if (!("class" in r) && i) {
          t.setAttribute("class", i);
          return;
        }
      }
      o.name in r || t.removeAttribute(o.name);
    }
  });
}
function R(t) {
  return t.nodeType === "Text" ? document.createTextNode(t.textContent) : document.createElement(t.tagName);
}
function Ht(t, e) {
  return e.nodeType === "Text" ? t instanceof Text : t instanceof HTMLElement && t.tagName.toLowerCase() === e.tagName;
}
function Ft(t, e) {
  const r = Array.from(t.childNodes).filter(Y), n = v(e.children), o = Math.min(r.length, n.length);
  for (let i = 0; i < o; i++) {
    const s = r[i], c = n[i];
    if (Ht(s, c))
      A(s, c);
    else {
      const a = R(c);
      t.replaceChild(a, s), A(a, c);
    }
  }
  for (let i = o; i < n.length; i++) {
    const s = n[i], c = R(s), a = Array.from(t.childNodes).find(et);
    t.insertBefore(c, a ?? null), A(c, s);
  }
  for (let i = r.length - 1; i >= o; i--)
    t.removeChild(r[i]);
}
const O = 16, d = 512, it = /[\u0000-\u001f\u007f]/, le = 150;
function Vt() {
  return `hsl(${Math.floor(Math.random() * 360)}, 70%, 60%)`;
}
function j(t) {
  return t !== null && typeof t == "object" && !Array.isArray(t);
}
function Bt(t) {
  if (!j(t) || !D(t.publicKey))
    return null;
  const e = j(t.playerStyle) ? t.playerStyle : {}, r = Array.isArray(e.colorPalette) ? e.colorPalette.filter(D).slice(0, O) : [], n = {
    publicKey: t.publicKey,
    playerStyle: { colorPalette: r }
  }, o = $(t.name);
  o !== void 0 && (n.name = o);
  const i = $(e.cursorStyle);
  return i !== void 0 && (n.playerStyle.cursorStyle = i), Number.isFinite(t.createdAt) && (n.createdAt = Number(t.createdAt)), n;
}
function D(t) {
  return typeof t == "string" && t.length > 0 && t.length <= d && !it.test(t);
}
function $(t) {
  if (typeof t == "string" && !it.test(t))
    return t.slice(0, d);
}
function Rt() {
  const t = crypto.getRandomValues(new Uint8Array(16)).reduce((n, o) => n + o.toString(16).padStart(2, "0"), ""), e = Math.floor(Math.random() * 360), r = [
    `hsl(${e}, 70%, 60%)`,
    `hsl(${(e + 120) % 360}, 70%, 60%)`,
    `hsl(${(e + 240) % 360}, 70%, 60%)`
  ];
  return {
    publicKey: t,
    playerStyle: {
      colorPalette: r
    },
    createdAt: Date.now()
  };
}
function jt(t) {
  const e = t.playerStyle?.colorPalette?.[0];
  return typeof e == "string" && e.length > 0;
}
function z(t) {
  try {
    localStorage.setItem(
      st,
      JSON.stringify(t)
    );
  } catch (e) {
    console.warn("Failed to save player identity to localStorage:", e);
  }
}
function Dt(t) {
  return jt(t) ? !1 : (t.playerStyle || (t.playerStyle = { colorPalette: [] }), Array.isArray(t.playerStyle.colorPalette) || (t.playerStyle.colorPalette = []), t.playerStyle.colorPalette[0] = Vt(), !0);
}
const st = "playhtml_player_identity";
let w = null;
function ue() {
  if (w) return w;
  const t = localStorage.getItem(st);
  if (t)
    try {
      const r = JSON.parse(t), n = Bt(r);
      if (n)
        return (Dt(n) || JSON.stringify(n) !== JSON.stringify(r)) && z(n), w = n, n;
    } catch {
      console.warn(
        "Failed to parse stored player identity, generating new one"
      );
    }
  const e = Rt();
  return z(e), w = e, e;
}
const ct = 512;
function fe(t) {
  return t === "cursor" ? "frame" : t.startsWith("element:") ? "interactive" : "event";
}
function de(t) {
  if (!y(t))
    throw new Error("Presence message must be an object");
  switch (t.type) {
    case "presence-join":
      return $t(t), t;
    case "presence-update":
      return J(t.channel), zt(t.channel, t.value), t;
    case "presence-clear":
      return J(t.channel), t;
    case "presence-ping":
      return t;
    default:
      throw new Error("Unsupported presence message type");
  }
}
function $t(t) {
  ft(t), x(t.page, "page", ct), t.identity !== void 0 && at(t.identity);
}
function zt(t, e) {
  if (e === void 0)
    throw new Error("Presence value must not be undefined");
  ft(e), t === "cursor" && Kt(e), t === "identity" && at(e);
}
function Kt(t) {
  if (!y(t))
    throw new Error("cursor presence value must be an object");
  if (!("cursor" in t))
    throw new Error("cursor presence value must include cursor");
  if (t.cursor !== null && Jt(t.cursor), t.zone !== void 0 && t.zone !== null && Ut(t.zone), x(t.page, "page", ct), t.at !== void 0 && !Number.isFinite(t.at))
    throw new Error("cursor at must be a finite number");
}
function he(t) {
  try {
    return lt(t), !0;
  } catch {
    return !1;
  }
}
function at(t) {
  lt(t);
}
function lt(t) {
  if (!y(t))
    throw new Error("identity must be an object");
  if (K(
    t,
    ["publicKey", "name", "playerStyle", "createdAt"],
    "identity"
  ), P(
    t.publicKey,
    "identity.publicKey",
    d
  ), !y(t.playerStyle))
    throw new Error("identity.playerStyle must be an object");
  K(
    t.playerStyle,
    ["colorPalette", "cursorStyle"],
    "identity.playerStyle"
  );
  const e = t.playerStyle.colorPalette;
  if (!Array.isArray(e))
    throw new Error("identity.playerStyle.colorPalette must be an array");
  if (e.length > O)
    throw new Error(
      `identity.playerStyle.colorPalette must have ${O} colors or less`
    );
  P(
    e[0],
    "identity.playerStyle.colorPalette[0]",
    d
  );
  for (let r = 1; r < e.length; r++)
    P(
      e[r],
      `identity.playerStyle.colorPalette[${r}]`,
      d
    );
  if (x(
    t.name,
    "identity.name",
    d
  ), x(
    t.playerStyle.cursorStyle,
    "identity.playerStyle.cursorStyle",
    d
  ), t.createdAt !== void 0 && !Number.isFinite(t.createdAt))
    throw new Error("identity.createdAt must be a finite number");
}
function K(t, e, r) {
  for (const n of Object.keys(t))
    if (!e.includes(n))
      throw new Error(`${r} must only include public presence fields`);
}
function ye(t) {
  try {
    return ut(t), !0;
  } catch {
    return !1;
  }
}
function Jt(t) {
  ut(t);
}
function ut(t) {
  if (!y(t))
    throw new Error("cursor must be an object");
  if (!Number.isFinite(t.x))
    throw new Error("cursor.x must be a finite number");
  if (!Number.isFinite(t.y))
    throw new Error("cursor.y must be a finite number");
  if (typeof t.pointer != "string" || t.pointer.length === 0)
    throw new Error("cursor.pointer must be a non-empty string");
}
function Ut(t) {
  if (!y(t))
    throw new Error("cursor zone must be an object");
  if (typeof t.zoneId != "string" || t.zoneId.length === 0)
    throw new Error("cursor zoneId must be a non-empty string");
  if (!Number.isFinite(t.relX))
    throw new Error("cursor zone relX must be a finite number");
  if (!Number.isFinite(t.relY))
    throw new Error("cursor zone relY must be a finite number");
}
function J(t) {
  if (typeof t != "string" || t.length === 0)
    throw new Error("Presence channel must be a non-empty string");
  V(t, "Presence channel", 128);
}
function P(t, e, r) {
  if (typeof t != "string" || t.length === 0)
    throw new Error(`${e} must be a non-empty string`);
  V(t, e, r);
}
function x(t, e, r) {
  if (t !== void 0) {
    if (typeof t != "string")
      throw new Error(`${e} must be a string`);
    V(t, e, r);
  }
}
function V(t, e, r) {
  if (t.length > r)
    throw new Error(`${e} must be ${r} characters or less`);
  if (/[\u0000-\u001f\u007f]/.test(t))
    throw new Error(`${e} must not contain control characters`);
}
function ft(t) {
  try {
    if (JSON.stringify(t) === void 0)
      throw new Error("Presence value must be JSON-serializable");
  } catch {
    throw new Error("Presence value must be JSON-serializable");
  }
}
function y(t) {
  return t !== null && typeof t == "object" && !Array.isArray(t);
}
function I(t) {
  return t !== null && typeof t == "object" && Object.getPrototypeOf(t) === Object.prototype;
}
function U(t, e) {
  if (e != null) {
    if (Array.isArray(e)) {
      t.splice(0, t.length, ...e);
      return;
    }
    if (I(e)) {
      for (const r of Object.keys(t))
        r in e || delete t[r];
      for (const [r, n] of Object.entries(e))
        Array.isArray(n) ? (Array.isArray(t[r]) || (t[r] = []), U(t[r], n)) : I(n) ? (I(t[r]) || (t[r] = {}), U(t[r], n)) : t[r] = n;
      return;
    }
    t = e;
  }
}
function pe(t) {
  try {
    if (typeof structuredClone == "function")
      return structuredClone(t);
  } catch {
  }
  return t == null ? t : typeof t == "object" ? JSON.parse(JSON.stringify(t)) : t;
}
function me(t) {
  const [e, r] = t.split("#");
  if (!e || !r)
    throw new Error("Invalid data-source attribute value");
  const n = e.indexOf("/"), o = n === -1 ? e : e.slice(0, n), i = n === -1 ? "/" : e.slice(n);
  return { domain: o, path: i, elementId: r };
}
const Gt = "LOCAL";
function Wt(t) {
  return t ? t.replace(/^www\./i, "") : Gt;
}
function qt(t) {
  if (!t) return "/";
  const e = t.replace(/\.[^/.]+$/, "");
  return e.startsWith("/") ? e : `/${e}`;
}
function be(t, e) {
  const r = Wt(t), n = qt(e);
  return encodeURIComponent(`${r}-${n}`);
}
function C(t) {
  return Math.round(t * 10) / 10 + 0;
}
function p(t) {
  const e = t?.trim();
  if (!e) return null;
  const r = e.startsWith("#") ? e.slice(1) : e;
  return document.getElementById(r) ?? document.querySelector(e);
}
function G(t) {
  return p(t.getAttribute(ne));
}
const W = 1, q = 60;
function Zt(t) {
  const e = t.getAttribute(oe);
  if (e == null) return W;
  const r = parseFloat(e);
  return Number.isFinite(r) ? Math.max(0, Math.min(1, r)) : W;
}
function Qt(t) {
  const e = t.getAttribute(ie);
  if (e == null) return q;
  const r = parseFloat(e);
  return !Number.isFinite(r) || r < 0 ? q : r;
}
function Z(t, e, r) {
  return Math.min(t, Math.max(t * e, r));
}
function _(t, e, r) {
  const n = t.getBoundingClientRect(), o = e.getBoundingClientRect();
  return {
    x: n.left - o.left - e.clientLeft - r.x,
    y: n.top - o.top - e.clientTop - r.y
  };
}
function te(t, e, r, n, o = _(t, e, r)) {
  const i = Zt(t), s = Qt(t), c = t.offsetWidth, a = t.offsetHeight, l = Z(c, i, s), u = Z(a, i, s), f = l - c - o.x, b = e.clientWidth - l - o.x, g = u - a - o.y, E = e.clientHeight - u - o.y;
  return {
    x: b < f ? 0 : Math.min(b, Math.max(f, n.x)),
    y: E < g ? 0 : Math.min(E, Math.max(g, n.y))
  };
}
function ee(t) {
  return {
    x: C(t.x),
    y: C(t.y)
  };
}
const re = "can-duplicate-to", ne = "can-move-bounds", oe = "can-move-bounds-min-visible", ie = "can-move-bounds-min-visible-px";
var se = /* @__PURE__ */ ((t) => (t.CanPlay = "can-play", t.CanMove = "can-move", t.CanSpin = "can-spin", t.CanGrow = "can-grow", t.CanToggle = "can-toggle", t.CanDuplicate = "can-duplicate", t.CanHover = "can-hover", t.CanMirror = "can-mirror", t))(se || {});
function ge(t) {
  return t.getAttribute("data-source") ? ce(t) : t.id;
}
function ce(t) {
  const e = t.getAttribute("data-source");
  if (!e)
    throw new Error("Element has no data-source attribute");
  const [r, n] = e.split("#");
  if (!r || !n)
    throw new Error("Invalid data-source attribute");
  return n;
}
const dt = `url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg'  width='44' height='53' viewport='0 0 100 100' style='fill:black;font-size:26px;'><text y='40%'>🚿</text></svg>")
      16 0,
    auto`, ae = `url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg'  width='40' height='48' viewport='0 0 100 100' style='fill:black;font-size:24px;'><text y='50%'>✂️</text></svg>") 16 0,auto`;
function Q(t, { getData: e, getElement: r, getLocalData: n, setLocalData: o }) {
  const i = e(), s = n(), c = r();
  if (s.isHovering = !0, t.altKey) {
    if (i.scale <= 0.5) {
      c.style.cursor = "not-allowed";
      return;
    }
    c.style.cursor = ae;
  } else {
    if (i.scale >= i.maxScale) {
      c.style.cursor = "not-allowed";
      return;
    }
    c.style.cursor = dt;
  }
  o(s);
}
function S(t) {
  if ("touches" in t) {
    const { clientX: e, clientY: r } = t.touches[0];
    return { clientX: e, clientY: r };
  }
  return { clientX: t.clientX, clientY: t.clientY };
}
const Ee = {
  "can-move": {
    defaultData: { x: 0, y: 0 },
    defaultLocalData: { startMouseX: 0, startMouseY: 0 },
    updateElement: ({ element: t, data: e }) => {
      t.style.transform = `translate(${e.x}px, ${e.y}px)`;
    },
    onDragStart: (t, { data: e, element: r, setLocalData: n }) => {
      const { clientX: o, clientY: i } = S(t), s = G(r), c = s ? _(r, s, e) : void 0;
      n({
        startMouseX: o,
        startMouseY: i,
        dragX: e.x,
        dragY: e.y,
        boundsBaseX: c?.x,
        boundsBaseY: c?.y
      });
    },
    onDrag: (t, { data: e, localData: r, setData: n, setLocalData: o, element: i }) => {
      const { clientX: s, clientY: c } = S(t), a = e.x + s - r.startMouseX, l = e.y + c - r.startMouseY, u = G(i);
      if (u) {
        const h = {
          x: r.dragX ?? e.x,
          y: r.dragY ?? e.y
        }, pt = h.x + s - r.startMouseX, mt = h.y + c - r.startMouseY, M = r.boundsBaseX !== void 0 && r.boundsBaseY !== void 0 ? { x: r.boundsBaseX, y: r.boundsBaseY } : _(i, u, h), L = te(
          i,
          u,
          h,
          {
            x: pt,
            y: mt
          },
          M
        ), T = ee(L);
        n(T);
        const bt = L.x - h.x, gt = L.y - h.y;
        o({
          startMouseX: r.startMouseX + bt,
          startMouseY: r.startMouseY + gt,
          dragX: T.x,
          dragY: T.y,
          boundsBaseX: M.x,
          boundsBaseY: M.y
        });
        return;
      }
      const { top: f, left: b, bottom: g, right: E } = i.getBoundingClientRect(), ht = window.visualViewport?.width ?? window.innerWidth, yt = window.visualViewport?.height ?? window.innerHeight;
      E > ht && s > r.startMouseX || g > yt && c > r.startMouseY || b < 0 && s < r.startMouseX || f < 0 && c < r.startMouseY || (n({
        x: C(a),
        y: C(l)
      }), o({ startMouseX: s, startMouseY: c }));
    },
    resetShortcut: "shiftKey"
  },
  "can-spin": {
    defaultData: { rotation: 0 },
    defaultLocalData: { startMouseX: 0 },
    updateElement: ({ element: t, data: e }) => {
      t.style.transform = `rotate(${e.rotation}deg)`;
    },
    onDragStart: (t, { setLocalData: e }) => {
      const { clientX: r } = S(t);
      e({
        startMouseX: r
      });
    },
    onDrag: (t, { data: e, localData: r, setData: n, setLocalData: o }) => {
      const { clientX: i } = S(t);
      let s = Math.abs(i - r.startMouseX) * 2, c = e.rotation;
      i > r.startMouseX ? c += s : i < r.startMouseX && (c -= s), n({ rotation: c }), o({ startMouseX: i });
    },
    resetShortcut: "shiftKey"
  },
  "can-toggle": {
    defaultData: { on: !1 },
    updateElement: ({ element: t, data: e }) => {
      const r = typeof e == "object" ? e.on : e;
      t.classList.toggle("toggled", r), t.classList.toggle("clicked", r);
    },
    onClick: (t, { data: e, setData: r }) => {
      const n = typeof e == "object" ? e.on : e;
      r({ on: !n });
    },
    resetShortcut: "shiftKey"
  },
  "can-grow": {
    defaultData: { scale: 1 },
    defaultLocalData: { maxScale: 2, isHovering: !1 },
    updateElement: ({ element: t, data: e }) => {
      t.style.transform = `scale(${e.scale})`;
    },
    onClick: (t, { data: e, element: r, setData: n, localData: o }) => {
      let { scale: i } = e;
      if (t.altKey) {
        if (e.scale <= 0.5)
          return;
        i -= 0.1;
      } else {
        if (r.style.cursor = dt, e.scale >= o.maxScale)
          return;
        i += 0.1;
      }
      n({ ...e, scale: i });
    },
    onMount: (t) => {
      const e = t.getElement();
      let r = !1;
      const n = (s) => Q(s, t), o = (s) => {
        Q(s, t), !r && (r = !0, document.addEventListener("keydown", n), document.addEventListener("keyup", n));
      }, i = () => {
        r && (r = !1, document.removeEventListener("keydown", n), document.removeEventListener("keyup", n));
      };
      e.addEventListener("mouseenter", o), e.addEventListener("mouseleave", i);
    },
    resetShortcut: "shiftKey"
  },
  // TODO: add ability to add max # of duplicates
  // TODO: add lifespan to automatically prune
  // TODO: add limit per person / per timeframe.
  "can-duplicate": {
    defaultData: [],
    defaultLocalData: [],
    updateElement: ({ data: t, localData: e, setLocalData: r, element: n }) => {
      const o = n.getAttribute(
        "can-duplicate"
        /* CanDuplicate */
      ), i = p(o);
      let s = document.getElementById(e.slice(-1)?.[0]) ?? null;
      if (!i) {
        console.error(
          `Element ${o} not found. Cannot duplicate.`
        );
        return;
      }
      const c = p(
        n.getAttribute(re)
      );
      function a(u) {
        if (c) {
          c.appendChild(u);
          return;
        }
        i.parentNode.insertBefore(
          u,
          (s || i).nextSibling
        );
      }
      const l = new Set(e);
      for (const u of t) {
        if (l.has(u)) continue;
        const f = i.cloneNode(!0);
        Object.assign(f, { ...i }), f.id = u, a(f), e.push(u), window.playhtml.setupPlayElement(f), s = f;
      }
      r(e);
    },
    onClick: (t, { data: e, element: r, setData: n }) => {
      const o = p(
        r.getAttribute(
          "can-duplicate"
          /* CanDuplicate */
        )
      );
      if (!o) return;
      const i = o.id + "-" + Math.random().toString(36).substr(2, 9);
      n((s) => {
        s.push(i);
      });
    },
    isValidElementForTag: (t) => {
      const e = t.getAttribute(
        "can-duplicate"
        /* CanDuplicate */
      );
      return e ? (p(e) || console.warn(
        `can-duplicate element (${t.id}) duplicate element ("${e}") not found.`
      ), !0) : !1;
    }
  },
  // TODO: auto-duplicate :hover CSS rules to [data-playhtml-hover] via CSSOM
  // so users don't need to manually rewrite their hover styles.
  "can-hover": {
    defaultData: {},
    myDefaultAwareness: { hover: !1 },
    onMount: ({ getElement: t, setMyAwareness: e }) => {
      const r = t();
      r.addEventListener("mouseenter", () => {
        e({ hover: !0 }), r.setAttribute("data-playhtml-hover", "");
      }), r.addEventListener("mouseleave", () => {
        e({ hover: !1 }), r.removeAttribute("data-playhtml-hover");
      });
    },
    updateElement: () => {
    },
    updateElementAwareness: ({ element: t, awareness: e }) => {
      e.some((n) => n?.hover) ? t.setAttribute("data-playhtml-hover", "") : t.removeAttribute("data-playhtml-hover");
    }
  },
  "can-mirror": St
};
export {
  re as CanDuplicateTo,
  ne as CanMoveBounds,
  oe as CanMoveBoundsMinVisible,
  ie as CanMoveBoundsMinVisiblePx,
  Gt as LOCAL_HOST_IDENTIFIER,
  O as MAX_PLAYER_IDENTITY_COLORS,
  d as MAX_PLAYER_IDENTITY_STRING_LENGTH,
  ct as MAX_PRESENCE_PAGE_LENGTH,
  st as PLAYER_IDENTITY_STORAGE_KEY,
  le as PROXIMITY_THRESHOLD,
  se as TagType,
  Ee as TagTypeToElement,
  pe as clonePlain,
  U as deepReplaceIntoProxy,
  be as deriveRoomId,
  ue as generatePersistentPlayerIdentity,
  Rt as generatePlayerIdentity,
  ge as getIdForElement,
  fe as getPresenceChannelCadence,
  ce as getSharedElementId,
  ye as isCursor,
  I as isPlainObject,
  he as isPlayerIdentity,
  y as isPresenceRecord,
  Wt as normalizeHost,
  qt as normalizePath,
  me as parseDataSource,
  Bt as toPublicPlayerIdentity,
  de as validatePresenceClientMessage
};
