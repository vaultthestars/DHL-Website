import { InternalObservable, ObserverConnectionSource } from "./observable";
export declare class Observer {
    readonly trigger: () => void;
    observing: Map<InternalObservable<object>, {
        iterate: false | true;
        byKey: Set<string | number>;
    }>;
    constructor(trigger: () => void);
    registerConnection<T extends object>(source: ObserverConnectionSource<T>): void;
    removeObservers(): void;
}
