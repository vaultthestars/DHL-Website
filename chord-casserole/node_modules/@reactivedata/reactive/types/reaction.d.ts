import { Observer } from "./observer";
export declare class Reaction extends Observer {
    private func;
    private options;
    private effect?;
    private isInitial;
    constructor(func: () => void | Promise<void>, options: {
        fireImmediately: boolean;
        name: string;
    }, effect?: () => void | Promise<void>);
    private reaction;
    private _trigger;
}
export declare function hasRunningReaction(): boolean;
export declare function runningReaction(): Reaction;
export declare function reaction(func: () => any | Promise<any>, effect: () => void | Promise<void>, options?: {
    fireImmediately?: boolean;
    name?: string;
}): Reaction;
